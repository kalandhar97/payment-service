apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: payment-processor
  labels:
    app: payment-service
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8087
      targetPort: http
  type: ClusterIP
